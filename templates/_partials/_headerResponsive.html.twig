<nav class="navbar navbar-responsive fixed-bottom navbar-expand-sm">
	<div class="container-fluid g-0">
		<div class="navbar-brand">
            <a href="{{ path('home.index') }}" class="fs-1 link-warning text-dark text-decoration-none ps-3">Snowtricks</a>
		</div>
		<button class="navbar-toggler me-2 border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon navbar-warning"></span>
		</button>

		<div class="collapse navbar-collapse bg-light" id="navbarNav">
			<ul class="navbar-nav">
				<li class="nav-item border-bottom border-1 border-dark">
					<a class="nav-link text-dark border-top border-2 border-dark fw-bold fs-4 ps-3" href="{{ path('home.index') }}">Home</a>
				</li>
				<li class="nav-item">
					<a class="nav-link text-dark fw-bold fs-4 ps-3" href="">Tricks</a>
				</li>
			</ul>
			<ul class="navbar-nav">
                {% if app.user %}
                    <li class="nav-item">
                        <div class="nav-link d-flex justify-content-between ps-3 pe-3 bg-warning">
							<div class="d-flex">
								<i class="fa-solid fa-circle-user text-dark" style="font-size:35px;"></i>
								<p class="d-flex text-dark my-auto ps-2 fs-5">{{ app.user.userIdentifier }}</p>
							</div>
                            <a class="nav-link" href="{{ path('security.logout') }}">
                                <i class="fa-solid fa-right-from-bracket text-danger"></i>
                            </a>
                        </div>
                    </li>
                {% else %}
				<li class="nav-item">
					<a class="nav-link link-info text-dark border-top border-1 border-dark bg-warning fw-bold fs-4 ps-3" href="{{ path('security.registration') }}">Sign up</a>
				</li>
				<li class="nav-item">
					<a class="nav-link link-info text-dark border-top border-1 border-dark bg-warning fw-bold fs-4 ps-3" href="{{ path('security.login') }}">Sign in</a>
				</li>
                {% endif %}
			</ul>
		</div>
	</div>
</nav>
